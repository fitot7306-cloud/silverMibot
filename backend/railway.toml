[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "node src/migrate.js && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
